// Next.js auto-generates <link rel="preload"> from <picture><source srcSet>.
// Additional manual preload removed to avoid duplicate links.
// LCP prioritization is handled by fetchPriority="high" on the <img> tag in Hero.tsx.
export default function HeroPreload() {
  return null;
}
